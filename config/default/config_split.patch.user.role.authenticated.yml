adding:
  dependencies:
    config:
      - filter.format.webform_default
    module:
      - fontawesome
      - media
      - search
  permissions:
    - 'access fontawesome additional settings'
    - 'search content'
    - 'use text format webform_default'
    - 'view media'
removing: {  }
